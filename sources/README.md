# Sources

Bookmarked external content flagged by `/scout` as 🟡 Bookmark — worth revisiting but not ready to write about now.

## Purpose

These are **raw material to potentially build from** — not curated examples to learn patterns from (that's `swipe-file/`). Each file is a Scout Report saved with frontmatter containing the source URL, score, pillar, and verdict.

## Naming

```
[slug].md
```

## Promotion

When a bookmarked source sparks a content idea worth developing, file it to `KB/30_Ideas/Content/` via `/cx-file-artifact`. This is a manual promotion, not an automatic dump.

## Related

- Created by: `/scout` workflow
- Evaluated by: `content-scout` skill
